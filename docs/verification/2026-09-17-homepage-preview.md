# 2026-09-17：单屏首页预览

这是本次验收的历史记录，操作步骤见 [发布说明](../release.md)，页面设计见 [设计方向](../specs/design.md)。

## 对应版本与部署

- 首页提交：`e8f07719a48565d40ca0ba48c3d97930686f39be`，分支 `codex/nuxt-foundation`。
- 使用 Wrangler 4.131.2 将已验证的 `.output/public` 直接上传到 Pages 项目 `ayingott-me`，上传时工作区干净。
- [本次预览](https://91fb8711.ayingott-me.pages.dev) 对应部署 `91fb8711-38e8-4eaf-92f1-cb103ea0c2d1`；Cloudflare API 回读确认环境为 `preview`、状态为 `success`，提交、分支与上传内容的未修改标记均匹配。

## 验证结果

- Node 24.19.0、pnpm 10.33.0 下的 `pnpm check` 与 `pnpm build` 通过。提交前源码未变化，复用该次检查和构建结果；生成目录中的 PDF 与原文件字节一致。
- Wrangler 本地预览通过：首页 `200`、缺失路由 `404`、PDF `200`，PDF 响应与原文件一致；桌面和手机布局、主题循环与持久化、键盘打开简历、404 返回首页均正常。
- 实际预览地址的 HTTP 验证通过：首页 `200`，`/__preview-check__/missing/` 返回 `404`，`/resume.pdf` 返回 `200` 和 `application/pdf`，响应字节与 `public/resume.pdf` 完全一致。
- 已检查 1440 × 900 桌面浅色和 390 × 844 手机深色截图。390px 与 320px 手机宽度均无横向溢出或多余纵向滚动；三个图标入口及与标题的对齐符合设计约定。
- 浏览器确认默认跟随系统、系统明暗变化同步、按钮按跟随系统 → 浅色 → 深色循环，刷新后保留选择。
- Tab 可到达简历入口且焦点可见，Enter 在新标签页打开本次预览的 PDF。缺失路由显示 404 页面，点击“回到首页”可恢复。
- 中文字体 400/500 字重、西文标题字体、应用脚本、样式及 GitHub 图标均加载成功，没有脚本异常或失败的应用资源请求。控制台的 404 和 `NUXT_E1005` 日志出现在主动访问缺失路由时。

## 发布边界

本次仅完成本地提交与分支预览，未推送 Git 分支、触发本版本的 Cloudflare Git 自动构建或发布生产环境。直接上传成功不代表本版本的云端 Git 构建已验证。

部署后回读确认生产分支仍为 `main`，生产部署仍为 `d43f84ff-cc67-4f2f-8fec-9950337c75cf`，正式域名配置保持原状。本次未新增长期验收脚本。
