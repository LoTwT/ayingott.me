# 2026-09-14：Agent 配置与文档整理

以下保留当时的文档结构与检查统计，不用于描述后续目录调整后的数量。

用户确认后，将 `AGENTS.md` 从符号链接改为普通文件；它与 `CLAUDE.md` 均逐字节匹配 `agent-config-setup` 模板。`docs/index.md` 从模板创建后，按文档整理要求补充导航。

- `docs/` 整理为六份文档，产品要求、开发约定、项目决策、部署说明和验收记录均由统一索引进入。
- 九份 Markdown 通过 Oxfmt 格式检查；52 条本地链接目标存在，旧目录引用无残留。
- 与整理前快照对比，应用代码、配置、脚本及简历 PDF 均未改动。

本次只验证文档结构、引用和文件一致性，未重复应用构建、浏览器验收或 Cloudflare 部署。同日工程结果见 [首轮工程验收](2026-09-14-foundation.md)。
